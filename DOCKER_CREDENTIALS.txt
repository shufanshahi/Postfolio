# POSTFOLIO DOCKER SERVICES CREDENTIALS
# =====================================
# These are the credentials for services running INSIDE Docker containers
# They are separate from your local PC services

# 1. POSTGRESQL DATABASE
# ----------------------
Host: localhost:5432 (from host) OR postgres:5432 (container-to-container)
Database: postfolio
Username: postgres
Password: admin

# 2. RABBITMQ MESSAGE QUEUE
# -------------------------
Host: localhost:5672 (AMQP) OR localhost:15672 (Management UI)
Username: guest
Password: guest
Management UI: http://localhost:15672
Note: These are the DEFAULT RabbitMQ credentials, not your PC's RabbitMQ

# 3. REDIS CACHE
# --------------
Host: localhost:6379
Password: (none - no authentication by default)

# 4. APPLICATION SERVICES
# -----------------------
Eureka Server: http://localhost:8761 (no auth)
API Gateway: http://localhost:8080 (no auth)
Main Backend: http://localhost:8082 (no auth)
AI Service: http://localhost:8081 (no auth)  
Stripe Service: http://localhost:9991 (no auth)

# 5. EXTERNAL API KEYS (from .env file)
# -------------------------------------
GEMINI_API_KEY: [Your Gemini API Key]
NEWS_API_KEY: [Your News API Key]
STRIPE_SECRET_KEY: [Your Stripe Secret Key]
STRIPE_PUBLIC_KEY: [Your Stripe Public Key]

# IMPORTANT NOTES:
# ===============
# - Docker containers use THEIR OWN isolated services
# - Your PC's RabbitMQ (admin/admin123) is separate from Docker RabbitMQ (guest/guest)
# - Docker services run on same ports but inside containers
# - You can change Docker credentials by modifying .env file